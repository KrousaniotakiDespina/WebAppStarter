@page
@model WebAppStarter.Pages.Teachers.SuccessModel
@{
    ViewData["Title"] = "Success Page";
}

<div class="container">

    <div class="mt-4 p-5 bg-success rounded text-center">
        <span class="display-3 text-white">Success!</span>
        <hr />
        <p>
            @Model.TeacherName <br />
            was successfully created
        </p>
        <a asp-page="/Teachers/ViewTeachers" class="btn btn-lg btn-primary">Back</a>
    </div>
</div>